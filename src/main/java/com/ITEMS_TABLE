CREATE TABLE ITEMS (
    ID NUMBER,
    CONSTRAINT ITEMS_PK PRIMARY KEY(ID),
    NAME NVARCHAR2(20),
    DATE_CREATED TIMESTAMP,
    DATE_LAST_UPD TIMESTAMP,
    DESCRIPTION NVARCHAR2(500)
);

CREATE SEQUENCE ITEM_SEQ
    MINVALUE 1
    START WITH 1
    INCREMENT BY 1
    CACHE 20